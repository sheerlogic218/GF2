prog def nonblocking_test : in.a len 4, in.b len 4 : out.y len 4;

    wire temp len 4;

    assign nonblocking wire.temp = in.a * in.b;
    assign nonblocking out.y     = wire.temp + in.b;

    monitor out.y;

end def;