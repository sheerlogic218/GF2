prog def module1 : in.a len 1 : out.y len 1;
    assign out.y = !in.a;
    monitor out.y;
end def;

prog def module2 : in.a len 1, in.b len 1 : out.y len 1;
    assign out.y = in.a + in.b;
    monitor out.y;
end def;